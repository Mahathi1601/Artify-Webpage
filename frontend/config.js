// config.js
const API_BASE_URL = 'https://artify-2iq3.onrender.com/api';

// Export for use in other files
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { API_BASE_URL };
}